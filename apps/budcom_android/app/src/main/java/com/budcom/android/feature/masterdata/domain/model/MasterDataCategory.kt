package com.budcom.android.feature.masterdata.domain.model

/**
 * Master-data categories exposed in the hub navigation.
 * Only [Ledgers] is implemented in this milestone.
 */
enum class MasterDataCategory {
    Ledgers,
    StockItems,
}

package com.budcom.android.domain.repository

/**
 * Marker for domain repository contracts.
 *
 * Implementations live under `data.repository` and are bound via Hilt.
 */
internal object DomainRepositoryPackage

<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Customer builds use the authenticated, certificate-pinned HTTPS transport. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>

package com.jajusri.venture.feature.sync.domain

/**
 * Android-derived Sync Foundation defaults (not Connector constants).
 */
object SyncDefaults {
    const val STATUS_POLL_INTERVAL_MS = 1_500L
    const val MAX_CONSECUTIVE_POLL_FAILURES = 5
    const val RUNS_PREVIEW_LIMIT = 5
}
